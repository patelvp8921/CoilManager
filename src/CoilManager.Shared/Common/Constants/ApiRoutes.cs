namespace CoilManager.Shared.Common.Constants;

public static class ApiRoutes
{
    public const string Root = "api";
    public const string Auth = $"{Root}/auth";
    public const string RawCoils = $"{Root}/rawcoils";
}
