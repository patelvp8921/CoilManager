import { DatePipe, DecimalPipe } from '@angular/common'; import { Component, OnInit, inject, signal } from '@angular/core'; import { FormControl, ReactiveFormsModule, Validators } from '@angular/forms'; import { MatButtonModule } from '@angular/material/button'; import { MatCardModule } from '@angular/material/card'; import { MatFormFieldModule } from '@angular/material/form-field'; import { MatIconModule } from '@angular/material/icon'; import { MatInputModule } from '@angular/material/input'; import { MatProgressBarModule } from '@angular/material/progress-bar'; import { MatSelectModule } from '@angular/material/select'; import { MatSnackBar, MatSnackBarModule } from '@angular/material/snack-bar'; import { MatTableModule } from '@angular/material/table'; import { ActivatedRoute, RouterLink } from '@angular/router';
import { AvailableCoil, CoilType, operationLabels, operationStatusLabels, productLabels, statusLabels, typeLabels, WorkOrder, WorkOrderStatus } from './work-order.model'; import { WorkOrderService } from './work-order.service';

@Component({selector:'app-work-order-detail',imports:[DatePipe,DecimalPipe,ReactiveFormsModule,RouterLink,MatButtonModule,MatCardModule,MatFormFieldModule,MatIconModule,MatInputModule,MatProgressBarModule,MatSelectModule,MatSnackBarModule,MatTableModule],template:`
@if(wo();as x){<section class="page"><header><div><a routerLink="/work-orders">← Work Orders</a><h1>{{x.workOrderNumber}} <span>{{statusLabels[x.status]}}</span></h1><p>{{typeLabels[x.workOrderType]}} · {{productLabels[x.productType]}}</p></div><div class="actions">@if(x.status===0){<a mat-stroked-button [routerLink]="['/work-orders',x.id,'edit']">Edit</a><button mat-flat-button (click)="action('release')">Release</button><button mat-button (click)="action('cancel')">Cancel</button>}@if(x.status===1){<button mat-flat-button (click)="action('start')">Start Work Order</button><button mat-button (click)="action('cancel')">Cancel</button>}@if(x.status===2){<button mat-flat-button (click)="action('complete')">Complete</button>}@if(x.status===3){<button mat-flat-button (click)="action('close')">Close</button>}</div></header>
<div class="summary"><mat-card><small>Required Weight</small><strong>{{x.requiredWeight||0|number:'1.0-3'}} kg</strong></mat-card><mat-card><small>Allocated Weight</small><strong>{{x.allocatedWeight|number:'1.0-3'}} kg</strong></mat-card><mat-card><small>Shortage Weight</small><strong>{{shortage(x)|number:'1.0-3'}} kg</strong></mat-card><mat-card><small>Issued / Consumed</small><strong>{{x.issuedWeight|number:'1.0-3'}} / {{x.consumedWeight|number:'1.0-3'}} kg</strong></mat-card></div>
<div class="grid"><mat-card><h2>Requirement</h2><dl><dt>Work Order Date</dt><dd>{{x.workOrderDate|date:'dd/MM/yyyy'}}</dd><dt>Required Date</dt><dd>{{x.requiredDate?(x.requiredDate|date:'dd/MM/yyyy'):'—'}}</dd><dt>Priority</dt><dd>P{{x.priority}}</dd><dt>Customer / Reference</dt><dd>{{x.customerName||x.salesOrderReference||'Inventory production'}}</dd><dt>Width / Quantity</dt><dd>{{x.requiredWidth||'—'}} mm / {{x.requiredQuantity||'—'}}</dd><dt>Drawing</dt><dd>{{x.drawingNumber||'—'}}</dd></dl></mat-card><mat-card><h2>Material Specification</h2><dl><dt>Grade</dt><dd>{{x.grade||'—'}}</dd><dt>Thickness</dt><dd>{{x.thickness}} mm</dd><dt>Category</dt><dd>{{x.category}}</dd><dt>Core Loss</dt><dd>{{x.coreLossPerKg}}</dd></dl><h3>Remarks</h3><p>{{x.remarks||'No remarks.'}}</p></mat-card></div>
<mat-card><h2>Operation Timeline</h2><div class="timeline">@for(op of x.operations;track op.id){<div [class.done]="op.status===3"><mat-icon>{{op.status===3?'check_circle':op.status===0?'remove_circle':'pending'}}</mat-icon><strong>{{operationLabels[op.operationType]}}</strong><span>{{operationStatusLabels[op.status]}}</span>@if(op.relatedDocumentNumber){<small>{{op.relatedDocumentNumber}}</small>}@if(op.operationType===0&&op.isRequired&&op.status===1&&x.status===1){<a mat-stroked-button [routerLink]="'/slitting-jobs/create'" [queryParams]="{workOrderId:x.id,workOrderOperationId:op.id,gradeId:x.gradeId,requiredWidth:x.requiredWidth,requiredWeight:x.requiredWeight}">Create Slitting Job</a>}</div>}</div></mat-card>
<mat-card id="allocations"><header class="section"><div><h2>Material Allocations</h2><p>Manual weight reservations; physical coil numbers and statuses remain unchanged.</p></div>@if(x.status===1||x.status===2){<div><button mat-stroked-button (click)="openAllocation(1)">Allocate Mother Coil</button><button mat-stroked-button (click)="openAllocation(2)">Allocate Slit Coil</button><button mat-button disabled>Automatic Allocation – Coming Later</button></div>}</header>
@if(allocating()){<div class="allocator"><mat-form-field><mat-label>Search inventory</mat-label><input matInput [formControl]="coilSearch" (keyup.enter)="searchCoils()"></mat-form-field><button mat-button (click)="searchCoils()">Search</button><mat-form-field><mat-label>Select specific coil</mat-label><mat-select [formControl]="selectedCoil">@for(c of coils();track c.id){<mat-option [value]="c.id">{{c.coilNumber}} · {{c.grade||'—'}} · {{c.width}} mm · {{c.availableWeight}} kg available</mat-option>}</mat-select></mat-form-field><mat-form-field><mat-label>Allocated Weight (kg)</mat-label><input matInput type="number" [formControl]="weight"></mat-form-field><button mat-flat-button (click)="allocate()" [disabled]="!allocationValid()">Add Allocation</button><button mat-button (click)="allocating.set(false)">Cancel</button>@if(selected()){<div class="coil-facts"><span>Current <b>{{selected()!.currentWeight}}</b></span><span>Reserved <b>{{selected()!.reservedWeight}}</b></span><span>Available <b>{{selected()!.availableWeight}}</b></span></div>}</div>}
<table mat-table [dataSource]="x.allocations"><ng-container matColumnDef="coil"><th mat-header-cell *matHeaderCellDef>Coil Number</th><td mat-cell *matCellDef="let a">{{a.coilNumber}}</td></ng-container><ng-container matColumnDef="type"><th mat-header-cell *matHeaderCellDef>Type</th><td mat-cell *matCellDef="let a">{{a.coilType===1?'Mother Coil':'Slit Coil'}}</td></ng-container><ng-container matColumnDef="weight"><th mat-header-cell *matHeaderCellDef>Allocated</th><td mat-cell *matCellDef="let a">{{a.allocatedWeight|number:'1.0-3'}} kg</td></ng-container><ng-container matColumnDef="remaining"><th mat-header-cell *matHeaderCellDef>Available After</th><td mat-cell *matCellDef="let a">{{a.remainingWeightAfterAllocation|number:'1.0-3'}} kg</td></ng-container><ng-container matColumnDef="status"><th mat-header-cell *matHeaderCellDef>Status</th><td mat-cell *matCellDef="let a">{{['Reserved','Issued','Partially Consumed','Consumed','Released'][a.status]}}</td></ng-container><ng-container matColumnDef="action"><th mat-header-cell *matHeaderCellDef></th><td mat-cell *matCellDef="let a">@if(a.status<3){<button mat-button (click)="release(a.id)">Release</button>}</td></ng-container><tr mat-header-row *matHeaderRowDef="allocationColumns"></tr><tr mat-row *matRowDef="let row;columns:allocationColumns"></tr></table></mat-card>
<div class="grid"><mat-card><h2>Related Slitting Jobs</h2>@for(job of x.relatedSlittingJobs;track job.id){<a [routerLink]="['/slitting-jobs',job.id,'edit']">{{job.slittingJobNumber}}</a>}@empty{<p>No linked jobs yet.</p>}</mat-card><mat-card><h2>Production Progress</h2><mat-progress-bar mode="determinate" [value]="progress(x)"></mat-progress-bar><p>{{progress(x)|number:'1.0-0'}}% of required operations complete.</p><p>Dispatch execution and Lamination Jobs are placeholders in this MVP.</p></mat-card></div></section>}`,styles:[`.page{padding:24px;display:grid;gap:16px}.page>header,.section{display:flex;justify-content:space-between;align-items:flex-start}h1 span{font-size:14px;background:#e8eefc;padding:6px 10px;border-radius:14px}.actions{display:flex;gap:8px}.summary{display:grid;grid-template-columns:repeat(4,1fr);gap:12px}.summary mat-card{padding:16px;display:grid;gap:8px}.summary strong{font-size:20px}.grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}mat-card{padding:20px}dl{display:grid;grid-template-columns:1fr 1fr;gap:10px}dd{margin:0;font-weight:600}.timeline{display:flex;gap:16px}.timeline>div{flex:1;padding:16px;border:1px solid #d8dee9;border-radius:10px;display:grid;gap:5px}.done{background:#edf9f1}.allocator{padding:16px;background:#f7f9fc;border-radius:10px;display:flex;gap:10px;align-items:center;flex-wrap:wrap}.coil-facts{display:flex;gap:16px}table{width:100%}@media(max-width:900px){.grid,.summary{grid-template-columns:1fr 1fr}.timeline{display:grid}}` ]})
export class WorkOrderDetailComponent implements OnInit {protected readonly statusLabels=statusLabels;protected readonly typeLabels=typeLabels;protected readonly productLabels=productLabels;protected readonly operationLabels=operationLabels;protected readonly operationStatusLabels=operationStatusLabels;protected readonly allocationColumns=['coil','type','weight','remaining','status','action'];protected readonly wo=signal<WorkOrder|null>(null);protected readonly allocating=signal(false);protected readonly allocationType=signal(CoilType.MotherCoil);protected readonly coils=signal<readonly AvailableCoil[]>([]);protected readonly coilSearch=new FormControl('',{nonNullable:true});protected readonly selectedCoil=new FormControl<string|null>(null,Validators.required);protected readonly weight=new FormControl<number|null>(null,[Validators.required,Validators.min(.001)]);private readonly service=inject(WorkOrderService);private readonly route=inject(ActivatedRoute);private readonly snack=inject(MatSnackBar);private id='';ngOnInit(){this.id=this.route.snapshot.paramMap.get('id')!;this.load();}protected load(){this.service.get(this.id).subscribe(x=>this.wo.set(x));}protected shortage(x:WorkOrder){return Math.max(0,(x.requiredWeight||0)-x.allocatedWeight)}protected progress(x:WorkOrder){const required=x.operations.filter(o=>o.isRequired);return required.length?100*required.filter(o=>o.status===3).length/required.length:100}protected action(name:string){if(name==='cancel'&&!confirm('Cancel this Work Order and release all active allocations?'))return;this.service.action(this.id,name).subscribe({next:x=>{this.wo.set(x);this.snack.open(`Work Order ${name}d.`,'Close',{duration:3000})},error:e=>this.snack.open(e?.error?.message||e.message,'Close',{duration:5000})});}protected openAllocation(type:CoilType){this.allocationType.set(type);this.allocating.set(true);this.searchCoils();}protected searchCoils(){this.service.available(this.id,this.allocationType(),this.coilSearch.value).subscribe(x=>{this.coils.set(x);this.selectedCoil.setValue(x[0]?.id??null)});}protected selected(){return this.coils().find(x=>x.id===this.selectedCoil.value)}protected allocationValid(){const coil=this.selected();return !!coil&&!!this.weight.value&&this.weight.value>0&&this.weight.value<=coil.availableWeight}protected allocate(){const coil=this.selected();if(!coil||!this.weight.value)return;this.service.allocate(this.id,this.allocationType(),coil.id,this.weight.value).subscribe({next:()=>{this.allocating.set(false);this.load();this.snack.open('Material allocated.','Close',{duration:3000})},error:e=>this.snack.open(e?.error?.message||e.message,'Close',{duration:5000})});}protected release(id:string){this.service.releaseAllocation(this.id,id).subscribe(()=>{this.load();this.snack.open('Allocation released.','Close',{duration:3000})});}}
