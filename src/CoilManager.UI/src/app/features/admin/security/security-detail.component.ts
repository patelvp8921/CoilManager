import { Component, inject, signal } from '@angular/core';import { DatePipe } from '@angular/common';import { ActivatedRoute, RouterLink } from '@angular/router';import { MatButtonModule } from '@angular/material/button';import { MatCardModule } from '@angular/material/card';import { MatChipsModule } from '@angular/material/chips';import { MatDialog, MatDialogModule } from '@angular/material/dialog';import { MatIconModule } from '@angular/material/icon';import { MatSnackBar, MatSnackBarModule } from '@angular/material/snack-bar';import { MatTabsModule } from '@angular/material/tabs';import { SecurityAdminService, UserDetail } from './security-admin.service';import { ResetPasswordDialogComponent } from './reset-password-dialog.component';import { Role } from '../../../core/auth/auth.models';
@Component({selector:'app-user-detail',imports:[DatePipe,RouterLink,MatButtonModule,MatCardModule,MatChipsModule,MatDialogModule,MatIconModule,MatSnackBarModule,MatTabsModule],templateUrl:'./user-detail.component.html',styleUrl:'./user-detail.component.scss'})
export class UserDetailComponent{private api=inject(SecurityAdminService);private route=inject(ActivatedRoute);private dialog=inject(MatDialog);private snackBar=inject(MatSnackBar);user=signal<UserDetail|null>(null);constructor(){this.api.user(this.route.snapshot.paramMap.get('id')!).subscribe(x=>this.user.set(x.data));}resetPassword(user:UserDetail){this.dialog.open(ResetPasswordDialogComponent,{data:{userId:user.id,displayName:user.displayName},width:'560px',maxWidth:'96vw',disableClose:true}).afterClosed().subscribe(reset=>{if(reset){this.snackBar.open('Password reset. The user must change it at next login.','Close',{duration:5000});this.api.user(user.id).subscribe(x=>this.user.set(x.data));}});}}
@Component({selector:'app-role-detail',imports:[RouterLink,MatButtonModule,MatCardModule,MatChipsModule,MatDialogModule,MatIconModule,MatSnackBarModule,MatTabsModule],template:`<section class="detail">@if(role();as r){<a mat-button routerLink="/admin/roles"><mat-icon>arrow_back</mat-icon>Roles</a><header><span class="avatar"><mat-icon>admin_panel_settings</mat-icon></span><div><h1>{{r.name}}</h1><p>{{r.description}}</p></div><button mat-stroked-button>Edit role</button></header><mat-tab-group><mat-tab label="General"><mat-card><h3>Role details</h3><p>{{r.assignedUsers}} assigned users · {{r.isSystem?'System role':'Custom role'}}</p></mat-card></mat-tab><mat-tab label="Permissions"><mat-card><div class="permission-list">@for(p of r.permissions;track p){<span><mat-icon>check_circle</mat-icon>{{p}}</span>}</div></mat-card></mat-tab><mat-tab label="Assigned users"><mat-card><p>{{r.assignedUsers}} users inherit permissions through this role.</p></mat-card></mat-tab></mat-tab-group>}</section>`,styleUrl:'./security-detail.component.scss'})
export class RoleDetailComponent{private api=inject(SecurityAdminService);private route=inject(ActivatedRoute);role=signal<Role|null>(null);constructor(){this.api.roles().subscribe(x=>this.role.set(x.data.find(r=>r.id===this.route.snapshot.paramMap.get('id'))??null));}}
