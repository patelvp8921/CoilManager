import { Component, inject, signal } from '@angular/core';import { FormControl,FormGroup,ReactiveFormsModule,Validators } from '@angular/forms';import { MatButtonModule } from '@angular/material/button';import { MatDialogModule,MatDialogRef } from '@angular/material/dialog';import { MatFormFieldModule } from '@angular/material/form-field';import { MatIconModule } from '@angular/material/icon';import { MatInputModule } from '@angular/material/input';import { SecurityAdminService } from './security-admin.service';
@Component({selector:'app-create-role-dialog',imports:[ReactiveFormsModule,MatButtonModule,MatDialogModule,MatFormFieldModule,MatIconModule,MatInputModule],template:`<h2 mat-dialog-title><mat-icon>add_moderator</mat-icon>Create role</h2><mat-dialog-content><p>Define a role, then assign its permissions from the role details page.</p><form id="role-form" [formGroup]="form" (ngSubmit)="save()"><mat-form-field appearance="outline"><mat-label>Role name</mat-label><input matInput formControlName="name"><mat-error>Role name is required</mat-error></mat-form-field><mat-form-field appearance="outline"><mat-label>Description</mat-label><textarea matInput rows="3" formControlName="description"></textarea></mat-form-field>@if(error()){<div class="error">{{error()}}</div>}</form></mat-dialog-content><mat-dialog-actions align="end"><button mat-stroked-button mat-dialog-close>Cancel</button><button mat-flat-button form="role-form" type="submit" [disabled]="form.invalid||saving()">{{saving()?'Creating…':'Create Role'}}</button></mat-dialog-actions>`,styles:[`h2{display:flex!important;gap:10px;align-items:center}mat-dialog-content{width:min(500px,80vw)}form{display:grid;gap:6px;padding-top:8px}.error{color:#ba1a1a}button{border-radius:6px}`]})
export class CreateRoleDialogComponent{private api=inject(SecurityAdminService);private ref=inject(MatDialogRef<CreateRoleDialogComponent>);saving=signal(false);error=signal('');form=new FormGroup({name:new FormControl('',{nonNullable:true,validators:[Validators.required,Validators.maxLength(100)]}),description:new FormControl<string|null>(null)});save(){if(this.form.invalid)return;this.saving.set(true);this.api.createRole(this.form.getRawValue()).subscribe({next:x=>this.ref.close(x.data),error:e=>{this.error.set(e.error?.errors?.join(' ')||e.error?.message||'Unable to create role.');this.saving.set(false);}});}}
